import { CuepointMediaDirective } from './cuepoint-media.directive';

describe('CuepointMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new CuepointMediaDirective();
    expect(directive).toBeTruthy();
  });
});
